import styled from "styled-components";

export const StyledRatingVideo = styled.div`
display:flex;
gap:10px;
& > div {
    display:flex;
    align-items:center;
    gap:7px;
};
align-items:center;
    & > div > span {
        font-size: 20px;
        cursor:pointer;
    }
`